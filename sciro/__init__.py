"""sciro -- PeakHub extensions to the Pybricks API.

These are typing stubs (like the `pybricks` package): the real modules are frozen
into the PeakHub firmware. Import from `sciro.parameters`, `sciro.iodevices` and
`sciro.pump`.
"""
